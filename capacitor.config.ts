import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-cap-barcode-scanner',
  webDir: 'www'
};

export default config;
